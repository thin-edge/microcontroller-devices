## Status

Implemented and **verified end-to-end on real ESP32-WROOM-32 hardware**: the
device joins Wi-Fi, runs the open62541 OPC-UA server, advertises itself over
mDNS/DNS-SD, and a standard OPC-UA client on the LAN discovers it by name
(`tedge-opcua.local`), browses the Device object, and reads live, updating
measurement values (stable 30 s / 15-read session, no disconnects).

Build/verify environment: Zephyr 4.4.2 + SDK 1.0.1 in the
`zephyrprojectrtos/zephyr-build` Docker image on macOS; firmware flashed to the
board from the host with `esptool`.

Not completed this session (see notes): ESP32-S2 and Pico W builds (the on-hand
WROOM was prioritised); the automated `native_sim` self-test (blocked by an NSOS
`select()` limitation on accepted sockets — hardware verification was used
instead).

## 1. Zephyr workspace and skeleton

- [x] 1.1 Initialize the Zephyr workspace: `west.yml` pins Zephyr v4.4.2 + open62541 v1.4.0; `west init`/`west update` documented in README
- [x] 1.2 Create the application skeleton (`CMakeLists.txt`, `prj.conf`, `src/main.c`) that boots and logs on `native_sim`
- [x] 1.3 ESP32-WROOM-32 (`esp32_devkitc/esp32/procpu`) builds, boots and logs over serial on real hardware; per-board `boards/*.conf` added — **ESP32-S2 build not attempted this session**
- [ ] 1.4 Add the Pico W board target and confirm it builds (stretch — not attempted; `boards/rpi_pico.conf` authored but unbuilt)
- [ ] 1.5 Enable PSRAM on the ESP32-S2 board config and confirm it is available (config authored; not built/verified — WROOM was prioritised)

## 2. Wi-Fi connectivity

- [x] 2.1 Kconfig options for Wi-Fi SSID/password + git-ignored `overlay-wifi-credentials.conf` mechanism (example + `.gitignore`)
- [x] 2.2 Wi-Fi station mode on ESP32-WROOM-32: associates and obtains an IP via DHCP (`192.168.68.74`), logged
- [~] 2.3 Raw TCP bring-up before OPC-UA: superseded — the full OPC-UA server was brought up directly and verified on WROOM. ESP32-S2/Pico W not done
- [x] 2.4 Wi-Fi reconnect-on-loss handling (implemented; reconnect observed during association)
- [x] 2.5 `native_sim` networking via NSOS (host-offloaded sockets); connectivity logged

## 3. open62541 integration

- [x] 3.1 open62541 vendored as a pinned amalgamation (`third_party/open62541/`) generated by `scripts/regen-open62541.sh`; compiled via the app `CMakeLists.txt`
- [x] 3.2 Minimal profile (MINIMAL namespace-0, no subscriptions/methods/discovery/history, single-threaded, 8 kB buffers) — compiles for `native_sim`
- [x] 3.3 Zephyr platform glue: NSOS `getnameinfo` shim (`src/nsos_compat.c`) + 8 amalgamation patches (IPv6 off, skip interrupt/UDP/Ethernet managers, neutralize `pipe()`, 8 kB RX buffer, NULL-host getaddrinfo fallback, monotonic clock via `k_uptime_get`)
- [x] 3.4 open62541 compiles/links for ESP32-WROOM-32; RAM/flash recorded (flash ~18%, dram ~64%; ~68 kB heap free after Wi-Fi) — fits after buffer/namespace tuning. **ESP32-S2/PSRAM not built**

## 4. OPC-UA server (capability: opcua-server)

- [x] 4.1 Server starts on the Kconfig-configurable `opc.tcp` port after the network is up
- [x] 4.2 Server runs in a dedicated Zephyr thread with a bounded stack; clean startup/shutdown
- [x] 4.3 Discovery + session create/activate verified — on **hardware** with a real client (asyncua); `native_sim` automated path blocked by NSOS `select()` on accepted sockets
- [~] 4.4 Bounded resources: max sessions/secure channels capped in config; graceful excess-connection rejection not explicitly stress-tested

## 5. Address space and data model (capability: device-data-model)

- [x] 5.1 Static Device object node with identity metadata (application/device name)
- [x] 5.2 Measurement variable nodes (temperature, humidity, pressure) with browse name, Double type, initial value
- [x] 5.3 Decoupled `data_source` module interface, independent of the OPC-UA layer
- [x] 5.4 Simulated data source producing changing, in-range synthetic values
- [x] 5.5 Periodic sampler at the Kconfig interval — driven from the server loop via `k_uptime_get()` (open62541's own timer clock is unreliable on Zephyr); verified values update live

## 6. mDNS / DNS-SD discovery (capability: mdns-discovery)

- [x] 6.1 Zephyr mDNS responder enabled; device reachable as `tedge-opcua.local` (verified from macOS `ping`)
- [x] 6.2 OPC-UA server advertised as `_opcua-tcp._tcp` DNS-SD service (verified via `dns-sd -B`)
- [~] 6.3 Re-announce on Wi-Fi reconnect/IP change — handled by Zephyr's responder; not explicitly tested across an IP change
- [x] 6.4 Discovery verified from macOS: `tedge-opcua.local` resolves, `dns-sd -B _opcua-tcp._tcp` lists the device, client connects by name

## 7. Build/config portability (capability: firmware-build-config)

- [x] 7.1 Tunables (port, device name, mDNS hostname via `NET_HOSTNAME`, sampling interval, Wi-Fi creds) exposed via Kconfig with `prj.conf` defaults
- [x] 7.2 Board-specific settings live in `boards/<board>.conf`; core app logic is board-independent
- [~] 7.3 Config change takes effect on rebuild — mechanism in place (Kconfig/overlays); not re-verified by toggling a value

## 8. Verification and interoperability

- [x] 8.1 macOS host tooling: `zephyrprojectrtos/zephyr-build` Docker container builds and runs `native_sim`; documented in README
- [ ] 8.2 Automated `native_sim` OPC-UA browse/read test — **blocked**: NSOS `select()` does not report readability on accepted sockets, so the handshake cannot complete under `native_sim`. Verified on hardware instead
- [ ] 8.3 Automated value-refresh test — same NSOS blocker; verified manually on hardware (values change across reads)
- [~] 8.4 Interoperability with a reference client — validated with the `asyncua` Python client (not UaExpert)
- [x] 8.5 Flashed ESP32-WROOM-32 from macOS and validated the full path over Wi-Fi: mDNS discover → connect → browse → read → live updates. **ESP32-S2 not done**
- [x] 8.6 Stability: 30 s / 15-read session with no disconnects (after the monotonic-clock fix); Wi-Fi reconnect observed at association

## 9. Documentation

- [x] 9.1 macOS build/flash instructions (Docker workspace, `esptool` flash at `0x1000`, serial port, blobs/submodules, unsupported-revision flag) — README updated
- [x] 9.2 Linux differences noted (native `native_sim`, serial device paths, `dialout` group)
- [x] 9.3 Wi-Fi credential configuration + mDNS discovery (`tedge-opcua.local` / `dns-sd`) + client connect documented
- [x] 9.4 Configuration options and how to add another Zephyr Wi-Fi board documented
