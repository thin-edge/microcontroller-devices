# SPDX-License-Identifier: Apache-2.0
#
# native_sim networking via NSOS (Native Simulator Offloaded Sockets): Zephyr
# BSD socket calls route straight to the host OS. Build/dev convenience only —
# end-to-end Modbus TCP is verified on hardware (NSOS has the same accept()/
# select() caveat noted for the OPC-UA app).
CONFIG_ETH_NATIVE_TAP=n
CONFIG_NET_DRIVERS=y
CONFIG_NET_SOCKETS=y
CONFIG_NET_SOCKETS_OFFLOAD=y
CONFIG_NET_NATIVE_OFFLOADED_SOCKETS=y
CONFIG_NET_IPV6_DAD=n
