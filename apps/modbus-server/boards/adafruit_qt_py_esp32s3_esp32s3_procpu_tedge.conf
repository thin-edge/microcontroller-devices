# SPDX-License-Identifier: Apache-2.0
#
# The Modbus QT Py ESP32-S3 build with thin-edge.io device management. Use on top of the
# board's own configuration:
#   west build --sysbuild -b adafruit_qt_py_esp32s3/esp32s3/procpu apps/modbus-server \
#     -- -DEXTRA_CONF_FILE="<this file>;<tenant>.conf"
#
# The tenant host belongs in a git-ignored overlay (CONFIG_TEDGE_C8Y_URL).

CONFIG_TEDGE=y
CONFIG_TEDGE_TRANSPORT_C8Y=y
CONFIG_TEDGE_C8Y_MQTT_SERVICE=y
CONFIG_TEDGE_AUTH_C8Y_CA=y
CONFIG_TEDGE_RESTART=y

# --- What the application provides for the client (tedge-zephyr/README.md) ---
CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_ENABLE_HEAP=y
CONFIG_MBEDTLS_HEAP_SIZE=98304
CONFIG_MBEDTLS_SSL_MAX_CONTENT_LEN=16384
CONFIG_MBEDTLS_SSL_PROTO_TLS1_2=y
CONFIG_MBEDTLS_CIPHERSUITE_TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256=y
CONFIG_MBEDTLS_CIPHERSUITE_TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256=y
CONFIG_PSA_WANT_ECC_SECP_R1_256=y
CONFIG_MBEDTLS_SSL_SERVER_NAME_INDICATION=y
CONFIG_MBEDTLS_HAVE_TIME_DATE=y
CONFIG_MBEDTLS_PEM_PARSE_C=y
CONFIG_MBEDTLS_X509_CRT_PARSE_C=y

# The device key lives in PSA ITS, stored through the settings backend.
CONFIG_SECURE_STORAGE_ITS_STORE_IMPLEMENTATION_SETTINGS=y

# --- The TLS heap goes in the board's quad PSRAM ---
# The N4R2 module has 2 MB of quad PSRAM where the DevKitC has 8 MB octal.
# Without this the 96 KB mbedTLS heap stays in internal DRAM and the image
# links at 99.8% of it, with 764 bytes to spare. Moving it frees 95,968 B
# and brings the board to 75.8%.
#
# SPIRAM_TYPE_* alone is not enough: it leaves CONFIG_ESP_SPIRAM_SIZE at 0,
# the ext_dram region is empty and the link fails with "External SPIRAM
# overflowed". The size has to be stated.
CONFIG_ESP_SPIRAM=y
CONFIG_SPIRAM_MODE_QUAD=y
CONFIG_SPIRAM_TYPE_ESPPSRAM16=y
CONFIG_ESP_SPIRAM_SIZE=2097152
CONFIG_MBEDTLS_HEAP_CUSTOM_SECTION=y

# One TLS context for MQTT, plus one per remote-access session.
CONFIG_NET_SOCKETS_TLS_MAX_CONTEXTS=2

# A closed TLS connection holds its TCP context for a moment, so the client
# needs headroom over what Modbus uses.
CONFIG_NET_MAX_CONN=10
CONFIG_NET_MAX_CONTEXTS=12
CONFIG_ZVFS_OPEN_MAX=14

# --- What a remote-access tunnel needs beside MQTT ---
# The Wi-Fi driver's receive path runs out of net_bufs under an interactive
# session long before it runs out of bandwidth, and websocket_send_msg()
# k_mallocs a masking buffer per frame. Sized as on the C6, which was
# verified against a real htop session. See
# tedge-zephyr/docs/debugging-against-real-devices.md.
CONFIG_HEAP_MEM_POOL_SIZE=40960
CONFIG_NET_BUF_DATA_SIZE=256
CONFIG_NET_PKT_RX_COUNT=16
CONFIG_NET_PKT_TX_COUNT=16
CONFIG_NET_BUF_RX_COUNT=96
CONFIG_NET_BUF_TX_COUNT=64
