# SPDX-License-Identifier: Apache-2.0
#
# Adafruit QT Py ESP32-S3 (dual-core Xtensa LX7, 512 KB SRAM, Wi-Fi + BT,
# 4 MB flash, 2 MB PSRAM). Build target: adafruit_qt_py_esp32s3/esp32s3/procpu.
# The tuned open62541 build fits the 512 KB internal SRAM, so PSRAM is not
# required here (use the board's @psram revision if it ever becomes tight).
# Mirrors the ESP32-WROOM config, including the connection-churn RAM/buffer
# tuning; the &wifi node is enabled via the sibling .overlay.

# --- Wi-Fi driver ---
CONFIG_WIFI=y
CONFIG_WIFI_ESP32=y
CONFIG_APP_WIFI=y

CONFIG_NET_MGMT=y
CONFIG_NET_MGMT_EVENT=y
CONFIG_NET_MGMT_EVENT_INFO=y
CONFIG_NET_CONNECTION_MANAGER=y

# open62541 uses the libc malloc arena (sized "all remaining RAM"); keep the
# separate k_heap small so it does not steal from that arena.
CONFIG_HEAP_MEM_POOL_SIZE=16384

# --- IP stack ---
CONFIG_NET_L2_ETHERNET=y
CONFIG_NET_IPV4=y
CONFIG_NET_IPV6=n
CONFIG_NET_UDP=y
CONFIG_NET_TCP=y
CONFIG_NET_DHCPV4=y

# --- mDNS / DNS-SD advertisement ---
CONFIG_NET_HOSTNAME_ENABLE=y
CONFIG_NET_HOSTNAME="tedge-opcua"
CONFIG_NET_HOSTNAME_UNIQUE=y
CONFIG_MDNS_RESPONDER=y
CONFIG_DNS_SD=y

# Socket / connection headroom, sized with margin so a burst of client
# (re)connections cannot exhaust contexts/file descriptors (which would wedge
# the event loop). Matches the WROOM tuning that survives the churn stress.
CONFIG_NET_MAX_CONTEXTS=12
CONFIG_NET_MAX_CONN=10
CONFIG_ZVFS_OPEN_MAX=16
CONFIG_NET_TCP_TIME_WAIT_DELAY=250

# --- Net buffer pools (deep enough that connection bursts don't starve the
#     Wi-Fi driver of RX buffers) ---
CONFIG_NET_PKT_RX_COUNT=10
CONFIG_NET_PKT_TX_COUNT=10
CONFIG_NET_BUF_RX_COUNT=40
CONFIG_NET_BUF_TX_COUNT=32
