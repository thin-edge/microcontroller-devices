# Telemetry is implemented, so it needs no experimental flag, and it brings
# nothing else in: an application that only wants measurements pays for those.
# expect: CONFIG_TEDGE_TELEMETRY=y
# expect: CONFIG_TEDGE_TELEMETRY_BUFFER_BYTES=2048
# expect: CONFIG_TEDGE_HEALTH=n
# expect: CONFIG_TEDGE_EXPERIMENTAL_FEATURES=n
# expect: CONFIG_TEDGE_HTTP=n
CONFIG_TEDGE_TELEMETRY=y
