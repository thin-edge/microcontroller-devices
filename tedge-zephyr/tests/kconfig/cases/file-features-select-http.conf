# Features that transfer files pull in the shared HTTP client. Parameters are
# not one of them: a change travels inside its operation.
# expect: CONFIG_TEDGE_LOG_UPLOAD=y
# expect: CONFIG_TEDGE_HTTP=y
CONFIG_TEDGE_LOG_UPLOAD=y
