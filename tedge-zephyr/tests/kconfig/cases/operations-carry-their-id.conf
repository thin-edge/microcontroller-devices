# Operations arrive as JSON by default, because that delivery carries the
# operation id, and the payload buffer grows to fit one.
# expect: CONFIG_TEDGE_C8Y_OPERATION_JSON=y
# expect: CONFIG_TEDGE_C8Y_PAYLOAD_BYTES=2048
