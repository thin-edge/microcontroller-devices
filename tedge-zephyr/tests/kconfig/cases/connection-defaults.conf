# The connection defaults of the direct transport.
# expect: CONFIG_TEDGE_C8Y_MQTT_SERVICE=y
# expect: CONFIG_TEDGE_SNTP=y
# expect: CONFIG_TEDGE_RECONNECT_BACKOFF_MAX_S=300
# expect: CONFIG_TEDGE_PSA_KEY_ID=0x0007E571
# expect: CONFIG_MQTT_LIB=y
# expect: CONFIG_NET_SOCKETS_SOCKOPT_TLS=y
# expect: CONFIG_TLS_CREDENTIALS=y
# expect: CONFIG_SETTINGS=y
