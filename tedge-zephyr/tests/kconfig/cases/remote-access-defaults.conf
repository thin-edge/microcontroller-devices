# Remote access with CA auth: LAN targets and a single session by default.
# expect: CONFIG_TEDGE_REMOTE_ACCESS=y
# expect: CONFIG_TEDGE_REMOTE_ACCESS_TARGETS_LAN=y
# expect: CONFIG_TEDGE_REMOTE_ACCESS_MAX_SESSIONS=1
# expect: CONFIG_TEDGE_HTTP=n
CONFIG_TEDGE_EXPERIMENTAL_FEATURES=y
CONFIG_TEDGE_REMOTE_ACCESS=y
