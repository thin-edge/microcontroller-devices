# Without TEDGE_EXPERIMENTAL_FEATURES, unimplemented features can't be
# selected. Configuration management is the last one left (roadmap P7).
# expect: CONFIG_TEDGE_CONFIG=n
# expect-warning: TEDGE_CONFIG .*TEDGE_FEATURE_AVAILABLE_CONFIG
CONFIG_TEDGE_CONFIG=y
