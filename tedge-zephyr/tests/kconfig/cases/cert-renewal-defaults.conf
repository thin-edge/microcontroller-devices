# Renewal with CA auth: a month of margin, an alarm a week before, daily checks.
# expect: CONFIG_TEDGE_CERT_RENEWAL=y
# expect: CONFIG_TEDGE_CERT_RENEW_BEFORE_DAYS=30
# expect: CONFIG_TEDGE_CERT_RENEW_ALARM_DAYS=7
# expect: CONFIG_TEDGE_CERT_RENEW_CHECK_HOURS=24
# expect: CONFIG_MBEDTLS_X509_CRT_PARSE_C=y
CONFIG_TEDGE_CERT_RENEWAL=y
