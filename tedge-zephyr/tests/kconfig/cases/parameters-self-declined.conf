# An image can take parameters for its application and decline the client's
# own set; the mechanism stays, the "tedge" set does not.
# expect: CONFIG_TEDGE_PARAMETERS=y
# expect: CONFIG_TEDGE_PARAMETERS_SELF=n
# expect: CONFIG_TEDGE_PARAMETERS_SCHEMA=y
CONFIG_SHELL=y
CONFIG_TEDGE_PARAMETERS=y
CONFIG_TEDGE_PARAMETERS_SELF=n
