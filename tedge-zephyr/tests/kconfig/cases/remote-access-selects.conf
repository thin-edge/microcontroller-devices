# Remote access brings in what the tunnel needs, and is off by default.
# expect: CONFIG_TEDGE_REMOTE_ACCESS=y
# expect: CONFIG_WEBSOCKET_CLIENT=y
# expect: CONFIG_PSA_WANT_ALG_SHA_1=y
# expect: CONFIG_TEDGE_HTTP=y
# expect: CONFIG_TEDGE_REMOTE_ACCESS_STACK_SIZE=8192
CONFIG_TEDGE_REMOTE_ACCESS=y
