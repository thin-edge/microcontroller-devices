# Health reports itself as measurements, so it turns telemetry on, and needs
# the reset reason and the heap statistics to have anything to say.
# expect: CONFIG_TEDGE_HEALTH=y
# expect: CONFIG_TEDGE_TELEMETRY=y
# expect: CONFIG_TEDGE_HEALTH_INTERVAL_S=900
# expect: CONFIG_HWINFO=y
# expect: CONFIG_SYS_HEAP_RUNTIME_STATS=y
CONFIG_TEDGE_HEALTH=y
