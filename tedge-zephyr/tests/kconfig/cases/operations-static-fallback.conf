# The way back for a tenant that does not serve the notification topic:
# static templates on s/ds, and a smaller payload buffer with them.
# expect: CONFIG_TEDGE_C8Y_OPERATION_JSON=n
# expect: CONFIG_TEDGE_C8Y_PAYLOAD_BYTES=1024
CONFIG_TEDGE_C8Y_OPERATION_JSON=n
