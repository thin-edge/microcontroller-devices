# Basic auth (bootstrap) builds fall back to Core MQTT automatically,
# because the MQTT Service refuses basic auth for devices.
# expect: CONFIG_TEDGE_AUTH_BOOTSTRAP=y
# expect: CONFIG_TEDGE_C8Y_CORE_MQTT=y
# expect: CONFIG_TEDGE_C8Y_MQTT_SERVICE=n
CONFIG_TEDGE_AUTH_BOOTSTRAP=y
