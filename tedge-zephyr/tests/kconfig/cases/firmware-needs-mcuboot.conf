# Firmware update cannot be selected without an MCUboot build.
# expect: CONFIG_TEDGE_FIRMWARE_UPDATE=n
# expect: CONFIG_IMG_MANAGER=n
# expect-warning: TEDGE_FIRMWARE_UPDATE .*BOOTLOADER_MCUBOOT
CONFIG_TEDGE_FIRMWARE_UPDATE=y
