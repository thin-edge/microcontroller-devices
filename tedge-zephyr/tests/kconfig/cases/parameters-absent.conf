# Without the feature there is no parameter set and no schema printer; the
# API calls are still there for an application to compile against, and
# return "not supported".
# expect: CONFIG_TEDGE_PARAMETERS=n
# expect: CONFIG_TEDGE_PARAMETERS_SCHEMA=n
CONFIG_TEDGE_TELEMETRY=y
